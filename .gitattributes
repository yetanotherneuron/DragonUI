# Exclude dev/repo files from GitHub release archives (git archive / zip downloads)
.gitignore export-ignore
.gitattributes export-ignore
.github/ export-ignore

# Texture and font formats are binary: never apply CRLF conversion or text diffs
*.blp binary
*.tga binary
*.dds binary
*.ttf binary
